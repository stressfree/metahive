<?xml version="1.0" encoding="UTF-8"?>

<beans:beans xmlns="http://www.springframework.org/schema/security"
    xmlns:beans="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
                        http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.0.xsd">

	<!-- HTTP security configurations -->
    <http auto-config="true" use-expressions="true">
    	<form-login login-processing-url="/resources/j_spring_security_check" login-page="/login" authentication-failure-url="/login?login_error=t"/>
        <logout logout-url="/resources/j_spring_security_logout"/>
        
        <!-- Configure these elements to secure URIs in your application -->
        <intercept-url pattern="/lists/**" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/datatypes/**" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/categories/**" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/resources/**" access="permitAll" />
        <intercept-url pattern="/**" access="permitAll" />
        
        <openid-login authentication-failure-url="/login?login_error=t" user-service-ref="openIdUserService" />    
        
        <access-denied-handler ref="accessDeniedHandler" />
    </http>

	<!-- Configure Authentication mechanism -->
    <authentication-manager alias="authenticationManager">
    	<!-- SHA-256 values can be produced using 'echo -n your_desired_password | sha256sum' (using normal *nix environments) -->
    	<authentication-provider>
	    	<password-encoder hash="sha"/>
	        <user-service>
	            <user name="admin" password="a94a8fe5ccb19ba61c4c0873d391e987982fbbd3" authorities="ROLE_ADMIN"/>
		        <user name="user" password="a94a8fe5ccb19ba61c4c0873d391e987982fbbd3" authorities="ROLE_USER"/>
		    </user-service>
    	</authentication-provider>
	</authentication-manager>
	
	<beans:bean id="openIdUserService" class="com.sfs.metahive.service.OpenIdUserDetailsService" />
	
	<beans:bean id="accessDeniedHandler" class="com.sfs.metahive.RedirectAccessDeniedHandler">
    	<beans:property name="accessDeniedUrl" value="accessdenied" />
	</beans:bean>
	
</beans:beans>